import chalk from "chalk";
//chalk is a library that allows you to select the color to display in the console message
console.log(chalk.green('Starting this app in dev mode...')); // eslint-disable-line no-console
